## LDAP

Разваорачиваем окружение:

```
vagrant up
```
Вагрант автоматически установит весь софт и обновит пакеты
Также не забудем указать в hosts адрес и FQDN домена
Выполняем конфигурацию клиента:
 ```
 ansible-playbook -i ./inventory ipa-client.yml
 ```
